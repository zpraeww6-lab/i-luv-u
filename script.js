/* ======================
   1️⃣ พิมพ์ข้อความทีละตัว
====================== */
const message =
  "เค้าอาจไม่เก่งคำพูด แต่ทุกวันที่มีเธอ คือวันที่เค้ามีความสุขที่สุดเลยนะ 🤍";
let index = 0;

function typeMessage() {
  index = 0;
  const textEl = document.getElementById("text");
  textEl.innerHTML = "";

  const typing = setInterval(() => {
    textEl.innerHTML += message[index];
    index++;
    if (index >= message.length) clearInterval(typing);
  }, 80);
}

/* ======================
   3️⃣ กล่องของขวัญ
====================== */
function openGift() {
  alert("เซอร์ไพรส์! 🎉\nเค้ารักเธอที่สุดในโลกเลย 💖");
}

/* ======================
   6️⃣ ปุ่มหนีความรัก
====================== */
function run() {
  const btn = document.getElementById("no");
  btn.style.left = Math.random() * 150 + "px";
  btn.style.top = Math.random() * 40 + "px";
}

function love() {
  alert("รู้อยู่แล้ว 😌💖");
}

/* ======================
   7️⃣ กล่องความทรงจำ
====================== */
const memories = [
  "เพราะเธอทำให้เรื่องธรรมดา กลายเป็นเรื่องพิเศษ ✨",
  "วันที่ไม่ได้คุยกันนานๆ ใจเค้าจะคิดถึงมากเลย 🥺",
  "ไม่ว่าจะเกิดอะไร เค้าอยากอยู่ข้างๆ เธอเสมอ 🤍"
];

function showMemory(i) {
  alert(memories[i]);
}